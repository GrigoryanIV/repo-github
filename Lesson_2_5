#Реализовать структуру «Рейтинг»
c = False
my_l = [9,8,7,4]

while c == False:
    num = input("Введите целое неотрицательное число: ")
    if num.isdigit():
        num = int(num)
        if num > 0:
            c = True
    else:
            print("Введённое значение должно быть целым неотрицательным числом")

    my_list = my_l.copy()
    for a in my_list:
        if a <= num:
            my_list.insert(my_list.index(a),num)
            break
        else:
            my_list.append(num)
            break

print(f"Рейтинг до внесения изменений: - {my_l} ")
print(f"Рейтинг после внесения изменений: - {my_list} ")